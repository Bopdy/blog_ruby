<h1>My Tasks</h1>

<ul style="list-style-type: none;" >
  <% @tasks.each do |task| %>
    <li>
      <%= check_box_tag("task", 'completed', task.completed, class:'filled-in') %>
      <%= link_to task.title, task %>
    </li>
  <% end %>
</ul>

<%= link_to 'Add a new task', new_task_path %>